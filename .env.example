# Copy this to .env and fill values for local dev
# In production (Render), set these in the dashboard Environment tab.

ADMIN_USER=admin
ADMIN_PASS=change-me-to-something-strong
SECRET_KEY=generate-a-random-32-char-string-here
DATABASE_PATH=./data.db
